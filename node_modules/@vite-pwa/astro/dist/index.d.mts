import { AstroIntegration } from 'astro';
import { VitePWAOptions } from 'vite-plugin-pwa';

interface PwaOptions extends Partial<VitePWAOptions> {
    experimental?: {
        /**
         * When using `generateSW` strategy, include custom directory and trailing slash handler.
         *
         * @see https://github.com/vite-pwa/astro/issues/23
         *
         * @default false
         */
        directoryAndTrailingSlashHandler?: boolean;
    };
}
declare function export_default(options?: PwaOptions): AstroIntegration;

export { type PwaOptions, export_default as default };
